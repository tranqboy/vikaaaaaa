# Сайт для Вікторії (статичний)

Це простий романтичний односторінковий сайт українською: зізнання, таймлайн, галерея і “лист” з ефектом друку + сердечка-сюрприз.

## Як запустити локально

### Варіант 1 (найпростіше)
- Відкрий `index.html` подвійним кліком у браузері.

### Варіант 2 (рекомендовано, щоб все було як на хостингу)
У терміналі в цій папці:

```bash
python -m http.server 5173
```

Потім відкрий `http://localhost:5173`.

## Як додати фото

1. Закинь фото в папку `photos/`.
2. Назви файли так:
   - `vika-1.jpg`
   - `vika-2.jpg`
   - ...
   - `vika-6.jpg`

Якщо фото немає — показується `photos/placeholder.svg`.

## Аналітика відвідувачів 📊

Сайт автоматично збирає дані про відвідувачів:
- **IP адреса** та геолокація (країна, місто)
- **Пристрій** (Mobile/Tablet/Desktop)
- **Браузер та ОС** (Chrome, Firefox, Safari, Windows, macOS, Android тощо)
- **Розмір екрана** та технічні характеристики
- **Мова браузера** та часовий пояс
- **Referrer** (звідки прийшов відвідувач)
- **Кліки** та взаємодії на сайті
- **Час візиту** та час на сайті

### Адмін-панель

Відкрий `admin.html` у браузері для перегляду статистики.

**Пароль за замовчуванням:** `vika2024` (зміни в `analytics.js` → `ADMIN_PASSWORD`)

Дані зберігаються локально в браузері (localStorage). Для збереження на сервері:
1. Створи webhook на [webhook.site](https://webhook.site) або іншому сервісі
2. Встав URL у `analytics.js` → `WEBHOOK_URL`

## Що легко кастомізувати

- Текст листа: `script.js` → змінна `letterText`
- Підпис: `index.html` → блок `id="signatureName"`
- Таймлайн “Наша історія”: `index.html` → секція `id="story"`
- Підписи під фото: `index.html` → `figcaption`
- Пароль адмін-панелі: `analytics.js` → `ADMIN_PASSWORD`
- Webhook для збереження даних: `analytics.js` → `WEBHOOK_URL`

## Хостинг (2 простих варіанти)

### Netlify (безкоштовно, дуже просто)
1. Зайди на Netlify, натисни “Add new site” → “Deploy manually”.
2. Перетягни всю папку проєкту (де є `index.html`).
3. Отримаєш посилання на сайт.

### GitHub Pages
1. Створи репозиторій на GitHub (public).
2. Завантаж файли в репозиторій.
3. Settings → Pages → Deploy from branch → `main` / root.
4. Сайт буде доступний за адресою GitHub Pages.

